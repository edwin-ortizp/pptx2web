"""GUI de administración (asistente de conversión) basada en pywebview.

Herramienta separada del conversor: NADA de este paquete acaba en la carpeta
exportada (el packager solo copia `player/`). La GUI es un front-end delgado
sobre el mismo núcleo que usa la CLI (`pptx2web.pipeline`).
"""
